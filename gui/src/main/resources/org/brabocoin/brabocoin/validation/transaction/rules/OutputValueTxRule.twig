{% extends "../../Rule.twig" %}

{% block description %}
    This rule checks that the sum of all outputs in the transaction is within the allowed range.
    If not, this transaction instance is invalid.<br>
    <br>
    Output sum: <code>{{ sum }}</code><br>
    Maximum money value: <code>{{ maxMoneyValue }}</code><br>
    <br>
    Check: 0 <= <code>{{ sum }} <= {{ maxMoneyValue }}</code><br>
{% endblock %}
