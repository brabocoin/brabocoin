{% extends "../../Rule.twig" %}

{% block description %}
    This rule checks whether the transaction's size is not greater than the maximum transaction size defined in consensus.
    If not, this transaction is invalid.<br>
    <br>
    Transaction size: <code>{{ size }}</code> <br>
    Consensus maximum size: <code>{{ consensusSize }}</code> <br>
    <br>
    Check: <code>{{ size }} <= {{ consensusSize }}</code><br>
{% endblock %}
