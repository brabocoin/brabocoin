{% extends "../../Rule.twig" %}

{% block description %}
    This rule checks whether the block height defined in the coinbase transaction of the block is the same as the block height defined in the block header.
    If not, the block is invalid.<br>
    <br>
    Coinbase transaction block height: <code>{{ coinbaseBlockHeight }}</code><br>
    Actual block height: <code>{{ blockHeight }}</code><br>
    <br>
    Check: <code>{{ coinbaseBlockHeight }} = {{ blockHeight }}</code><br>
{% endblock %}
