{% extends "../../Rule.twig" %}

{% block description %}
    This rule checks that the sum of all inputs in the transaction is within the allowed range.
    If not, this transaction instance is invalid.<br>
    <br>
    Input sum: <code>{{ sum }}</code><br>
    Maximum money value: <code>{{ consensusMaxMoneyValue }}</code><br>
    <br>
    Check: <code>0 <= {{ sum }} <= {{ consensusMaxMoneyValue }}</code><br>
{% endblock %}
