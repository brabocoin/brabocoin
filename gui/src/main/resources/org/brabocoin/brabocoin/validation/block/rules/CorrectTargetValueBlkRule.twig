{% extends "../../Rule.twig" %}

{% block description %}
    This rule checks whether the target value in the block is according to the target value defined in the configuration.<br>
    <br>
    Block target value: <code>{{ blockTargetValue }}</code><br>
    Configuration target value: <code>{{ configTargetValue }}</code><br>
    <br>
    Check:<br>
    <code>
        {{ configTargetValue }} = <br>
        {{ blockTargetValue }}
    </code><br>
{% endblock %}
