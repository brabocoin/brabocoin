This rule checks whether any of the inputs in the transactions within the block occur in more than one transaction.
If so, the block is invalid and should be rejected.<br>
<br>
Block has no duplicate inputs: {{ hasDuplicates }}