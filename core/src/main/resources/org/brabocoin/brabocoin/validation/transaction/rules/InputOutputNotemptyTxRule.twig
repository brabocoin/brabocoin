This rule checks that the transaction contains at least one input and one output.
If not, the transaction is invalid and should be rejected.<br>
<br>
Input count: {{ inputCount }}<br>
Output count: {{ outputCount }}<br>
<br>
Check:<code> {{ inputCount }} > 0 </code> and <code> {{ outputCount }} > 0 </code>